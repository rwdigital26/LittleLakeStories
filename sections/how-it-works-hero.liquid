<section class="how-it-works-hero" style="--how-it-works-hero-bg: {{ section.settings.background_color }};">
  <div class="how-it-works-hero__media">
    {% if section.settings.image != blank %}
      {{ section.settings.image | image_url: width: 2400 | image_tag: widths: '1200, 1600, 2000, 2400', sizes: '100vw', alt: section.settings.image.alt | default: section.settings.heading, loading: 'eager' }}
    {% else %}
      <img src="{{ 'how-it-works-hero.png' | asset_url }}" alt="A child and dog watching the sunset on a lake" width="1983" height="793" loading="eager">
    {% endif %}
  </div>
  <div class="how-it-works-hero__content">
    <h1>{{ section.settings.heading }}</h1>
    <p>{{ section.settings.subheading }}</p>
    {% if section.settings.description != blank %}<div class="how-it-works-hero__description">{{ section.settings.description }}</div>{% endif %}
  </div>
</section>

{% stylesheet %}
  .how-it-works-hero {
    position: relative;
    min-height: clamp(420px, 40vw, 640px);
    margin-bottom: 75px;
    overflow: hidden;
    background: var(--how-it-works-hero-bg, #eaf8fb);
    color: #143d63;
  }

  .how-it-works-hero__media,
  .how-it-works-hero__media img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }

  .how-it-works-hero__media img {
    display: block;
    object-fit: cover;
    object-position: center;
  }

  .how-it-works-hero__content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    width: min(46%, 680px);
    min-height: clamp(420px, 40vw, 640px);
    padding: clamp(40px, 7vw, 96px);
    background: linear-gradient(90deg, rgb(255 255 255 / 92%) 0%, rgb(255 255 255 / 74%) 70%, rgb(255 255 255 / 0%) 100%);
  }

  .how-it-works-hero h1,
  .how-it-works-hero p,
  .how-it-works-hero__description {
    max-width: 600px;
    font-family: Georgia, 'Times New Roman', serif;
  }

  .how-it-works-hero h1 {
    margin: 0;
    font-size: clamp(2.5rem, 5vw, 5.5rem);
    line-height: 0.98;
  }

  .how-it-works-hero p {
    margin: 18px 0 0;
    font-size: clamp(1.25rem, 2vw, 2rem);
    font-style: italic;
    font-weight: 700;
    line-height: 1.15;
  }

  .how-it-works-hero__description {
    margin-block-start: 20px;
    font-size: 1.05rem;
    font-weight: 700;
    line-height: 1.4;
  }

  @media screen and (max-width: 749px) {
    .how-it-works-hero {
      min-height: 620px;
      margin-bottom: 52px;
    }

    .how-it-works-hero__media img {
      object-position: 62% center;
    }

    .how-it-works-hero__content {
      justify-content: flex-start;
      width: 100%;
      min-height: 100%;
      padding: 48px 24px;
      background: linear-gradient(180deg, rgb(255 255 255 / 92%) 0%, rgb(255 255 255 / 64%) 54%, rgb(255 255 255 / 0%) 100%);
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "How It Works hero",
  "settings": [
    { "type": "image_picker", "id": "image", "label": "Hero image" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "How It Works" },
    { "type": "text", "id": "subheading", "label": "Subheading", "default": "From your memories to their favorite book." },
    { "type": "textarea", "id": "description", "label": "Description", "default": "Creating a one-of-a-kind Little Lake Story is easy. You choose the book, we'll guide you through the rest. In just a few simple steps, your child's real adventures become a beautifully illustrated keepsake." },
    { "type": "color", "id": "background_color", "label": "Background color", "default": "#eaf8fb" }
  ],
  "presets": [
    { "name": "How It Works hero" }
  ]
}
{% endschema %}
